SF Home Finder 0.5.24 for Windows x64

A home whose source publishes no date shows "Found Yesterday" in the Posted
column, and that was a word too wide for the column: it ran out under the
Listing column beside it and was cut off mid-word. It wraps onto two lines
now, with the date kept whole, at every window width.


SF Home Finder 0.5.23 for Windows x64

The board is faster everywhere, and passing a home no longer redraws it.

Every page was working out, for each of the eleven thousand homes it holds,
an answer that depends on nothing but that home's source: when that source
last searched everywhere it keeps homes. Two dozen answers, worked out eleven
thousand times, for every tab you opened. It asks each source once now.

Measured on a real board of 11,300 homes, in processor time so that what else
the machine is doing cannot flatter it: private rooms twice as fast, your
saved homes twice as fast, one-bedrooms and studios about three-quarters
faster, near matches a third faster, and the wait after pressing Pass down by
a third.

Pass itself no longer waits at all on the shortlist and in near matches: the
home leaves the list, the count in the heading drops by one, and the saving
happens behind it -- 40 milliseconds where it used to be about a second. On a
tab with more than one page it still reloads, because a home from the next
page moves up into the gap and only a reload knows which one. If the save is
refused the home comes straight back.

Nothing about what you see has changed: every tab, view, sort, filter and page
of the board renders exactly what it did before.


SF Home Finder 0.5.22 for Windows x64

Three days after it arrives, the app asks once whether you would like to help
pay for it -- a card over the board, rather than the quiet line in the footer
that has always been there. Close it and it asks once more a week later, and
then never again. Pressing Say thanks ends the asking immediately, whether or
not you give anything.

It stays out of the way to get there. It will not appear over a running check,
or while anything about the app itself needs attention, or in a window you are
not looking at. A listings site having a bad morning does not count as the app
being broken -- with twenty-three of them read twice a day, one is almost
always paused, and a card that waited for a spotless report would never appear
at all.

Say thanks opens the same Ko-fi panel the footer button has always opened.
Nothing is fetched from Ko-fi until you press it.


SF Home Finder 0.5.21 for Windows x64

Review your deal now describes the whole deal, and keeps up as you fill it in.
It used to freeze the moment you ticked a second home size and say nothing
about it, so the save afterwards refused for a reason the review had never
mentioned. A ticked size with no price is named, every area is named rather
than the first four, and the move-in dates, lease length and household limit
are all in the sentence. When an answer stops the deal making sense, the
sentence says which one instead of leaving the old description standing. It
also arrives while you type: it used to wait on a rescore of every home you
have collected.

Areas in the Avoid column are now actually read when a home is scored. The
column promised known listings would stay out, and nothing was reading it.

Near matches ends with two ways on -- the Other homes tab, and your deal --
instead of stopping.

Starring is immediate. It used to rebuild the whole list to fill in one star,
which took about three seconds with nothing on screen saying the click had
landed. The starred button no longer matches the row it marks, so you can see
which homes are starred.

Starred cards have space between them and are about two-thirds of the height.



SF Home Finder 0.5.20 for Windows x64

A rent far under what its area charges is marked "46% off", and the note behind
it says the line it crossed: anything more than 40% off the going rate. It read
"46% under market" before, against a rule written the other way round, which
made a correct mark look wrong.

The row counting the homes held back now has a button to the near matches.


SF Home Finder 0.5.19 for Windows x64

The mark on a rent far under its area now reads "46% under" on one line, and
the note behind it is a sentence shorter. It said "46% under market" before,
which wrapped onto two lines on every flagged row.


SF Home Finder 0.5.18 for Windows x64

A rent far below what its area charges for a home that size now carries a mark
you can open. It says what homes like it let for there, and that the figure can
be a real find or a scam. The home keeps its place on the board either way.

Movoto does not state how many bedrooms one home in seven has, and those homes
were the whole of the Other homes tab. Its floor area answers the question, and
the row says that is where the answer came from.

Facebook homes outside your target areas showed a postcode and a map pin where
their area should be.

Scrolling the board right no longer reaches a band of empty table past the last
column.


SF Home Finder 0.5.17 for Windows x64

Check for alerts now reports on the card you pressed it on. It used to start
the check and then move you to the dashboard, which is not where the answer
arrives. While the check runs the card shows real progress, and afterwards it
says when it last ran.

The four saved-search sites below it settle at the same time, and they used to
stay stale until you reloaded the page.


SF Home Finder 0.5.16 for Windows x64

The first Facebook check spends its hundred slots on homes inside your deal.
Three of the four feeds it reads went out yesterday with no price filter at
all, so a room at any rent could take a slot. Every feed now carries the same
price band, built from Your deal.

That band is a quarter either side of your range rather than a twentieth.
Facebook prices are asking prices and are often mislabelled -- a room
advertised at the whole flat's rent, a monthly figure entered as weekly -- and
the narrow band was discarding homes that read as matches on sight. Anything
outside your deal still ranks below anything inside it.

Every feed is read newest first.


SF Home Finder 0.5.15 for Windows x64

Facebook Marketplace was reading ten listings a scan and reporting nine. The
city Property Rentals feed is also the only place it looked, and rooms and
sublets are posted as ordinary Marketplace items that never reach that feed,
so a whole category was missing rather than merely short. Both are fixed: the
first check after connecting reads about a hundred listings to fill the board,
every check after it reads about ten to catch what is new, and rooms and
sublets are read alongside the rentals feed in the same check.

Pressing Test Facebook connection now checks Facebook. It used to start a scan
of all eighteen sources against one shared time limit, so the source the button
exists to check was regularly the one cut for time -- the dashboard would say
"Not checked this time" for Facebook immediately after you asked it to check
Facebook.

The card says what happened. It shows the result, when the check ran and what
it found, and while a check is running it shows real progress from the scan
rather than a badge that sits on Testing until you reload. A check interrupted
by a quit is reported as unfinished instead of still running, and a failure
arrives as a sentence rather than as its exception's class name.

Public Facebook groups have their own row, their own result and a month-long
window. They used to write into the Marketplace connector's status, so testing
groups overwrote the Marketplace answer and said nothing about the groups.

Saving the token is now the whole job: it starts the first check itself instead
of waiting for a second press nobody knew to make.


SF Home Finder 0.5.14 for Windows x64

Clicking a column heading sorts by it, and clicking it again turns it over.
Score, Price and Posted, with an arrow saying which way the column is sorted.
Reversing a column turns over that column and nothing else: a home its source
has stopped listing still sorts below the ones still showing, because that is
the board saying how far a row can be trusted rather than an order to flip.

Move-in has left the table. Seven of 439 homes on one real shortlist stated
one, so the column was a stack of "Not stated". The date is still collected,
still on the home's own row, and still in the Order menu. Posted takes its
place, next to the listing it belongs to.

The size and the bathrooms are one line -- "1 br / 1 bath" -- with the
building size quieter underneath, and the bathroom count now arrives from two
more sites. ApartmentGuide and Trulia both published it all along and neither
was read; between them that is 784 homes on one board that could only say
"Baths n/a". Where a building lets one-bath and two-bath homes alike it still
says nothing, because neither number describes the home you are looking at.


SF Home Finder 0.5.13 for Windows x64

A Zillow home is checked as it arrives, and Zillow is left alone the rest
of the time. Yesterday's release read twenty of its pages a scan to find the
ones taken down; Zillow blocks by address after about thirty, and the block
covers its search -- which is where every Zillow home comes from. So it reads
five, and the five it reads are the homes that have just arrived, checked
before they are ever shown as something worth acting on. If Zillow turns a
page away, the app stops asking for the rest of them instead of reading on
into the block.


SF Home Finder 0.5.12 for Windows x64

Homes Zillow has taken down leave the shortlist. Its search goes on
returning them as though they were still to let -- 502 Precita Ave and 161
Albion St were both arriving that way on the days their own pages read "Off
market" -- so the app now reads the home's own page and believes it only when
it says the same thing twice: nothing on it Zillow marks for rent, and a
marker you would see yourself. Of the twenty best-scoring Zillow homes on one
real shortlist, thirteen were off the market, including one showing at
$2,594 and scored "Excellent". A page that will not load says neither thing
and costs a home nothing, because a home that might still be there stays.

The Neighborhood filter offers the neighbourhoods that tab actually holds.
Every tab shared one list built from the whole board, so Near matches offered
the shortlist's areas and the Archive's, and choosing one returned an empty
page. On one board the list ran to hundreds of entries on a tab showing nine
areas; now it shows the nine.


SF Home Finder 0.5.11 for Windows x64

The Neighborhood filter offers neighbourhoods. It was built from whatever
each site had written in that field, so it listed Miami, New York, Chicago
and Daly City beside Castro and Nob Hill, along with a scattering of street
addresses. On a real board 169 of its 394 entries led nowhere, because a home
the app places outside San Francisco never reaches the shortlist anyway.
Those are gone. The area names people actually use, including Craigslist's
own "sunset / parkside" and "downtown / civic / van ness", all stay.


SF Home Finder 0.5.10 for Windows x64

Homes you cannot rent are off the shortlist. A Zillow page that has gone off
the market, or that asks you to claim the home, is not a listing and is kept
out. A city lottery leaves the day its deadline passes rather than sitting
there until somebody notices.

The price says where it came from. A rent the listing states is shown
plainly; one remembered from the last time the home appeared is dated; one
worked out from similar homes nearby is marked as an estimate. Each says so
in a line you can open.

A rent far below anything that size lets for is checked before it is
promoted. If the page is still up the home keeps its place, and if the page
is gone it is not the bargain at the top of your list.

Homes are named by the part of the city they are in. One whose listing gives
a street address but no neighbourhood is placed by the city's own address
records -- on a real board that named 208 homes the app had nothing to say
about, including the whole of the Sunset, which the coarser city layer cannot
tell apart from Parkside. An area already on a home is never quietly swapped
for another.

A home its source has stopped listing sinks after a day and a half and leaves
for Near matches after three. Two things had to be true for that to be fair.
Only a search that reads a whole site counts, and three sites were reading one
page of theirs and calling it San Francisco. And the days counted are days
somebody actually looked, so a laptop shut for a weekend no longer empties the
shortlist on Monday morning.

Nothing is deleted. What leaves the shortlist is in Near matches or the
archive, and homes you have saved or noted stay where they are.


SF Home Finder 0.5.9 for Windows x64

The install now says how far along it is, not just how long it has been
going. The step that takes the longest -- the one that gets everything ready
before the app starts -- counts its way to a hundred per cent as it loads,
which is a real count of the work rather than a bar moving on a timer.

A neighbourhood you have chosen is removed with a cross rather than the word
"Remove" beside every one of them, the note under the budgets is shorter, and
the shortlist no longer carries a sentence about how unpriced homes are
placed.


SF Home Finder 0.5.8 for Windows x64

Installing says what it is doing. Each step is one line with a tick and the
time it took, instead of forty-seven library names scrolling past, and the
line you are waiting on is the only one moving.

Upgrading no longer looks like it failed. A first start after an install
re-ranks every home already collected, and macOS vets a runtime it has never
seen, and nothing is served until both finish. On a real board of 9,615 homes
that took fifty seconds run plainly and seventy-three inside the background
service the app runs as -- six minutes on a machine still busy installing.
Past two and a half minutes the installer gave up and said it had not worked,
while it was working. That work now happens during the install, as one
visible step that nothing throttles, and the app itself then starts in three
and a half seconds.

A slow first start used to fail the install outright and send you to Repair
for an app that was starting normally. The work that made it slow is done
during the install now.


SF Home Finder 0.5.7 for Windows x64

Your data is much harder to lose. Mac and Windows now keep backups by one
set of rules. Repair restores the newest copy that opens cleanly and never
deletes the last one, and a board that is damaged is refused at start with a
message that says to run Repair, rather than a page that half works. This
matters most on the way up from an older version: Repair up to 0.5.6 deleted
the backups it was meant to restore from and restored nothing. This release's
Repair runs its own script, not the one an older install left behind.

The same home listed on several sites now appears once. Copies are joined only
on clear evidence, because merging two different flats would hide one of them;
a star or a pass reaches every copy, and the counts beside your cut-off count
homes rather than listings. Where a site states no rent, the typical rent of
homes the same size in the same neighbourhood is used to rank it, and is never
shown as though it were the asking price.

The sites are asked less, and more gently. A check refused partway keeps the
pages it had already read, errors are said in words, a site your deal does not
need is not asked at all, and a check that ran with no network no longer
counts against a source as though the source had failed.

It stays quick after a year of use. Measured on a board of 213,000 homes: the
dashboard opens in under half a second instead of nearly six, starting takes
3.3 seconds instead of 4.6, and a page of the archive about a second instead
of twenty. A home stays on the board for 21 days after it is found, then moves
to the archive; once no site lists it any more it is kept for 120 days and
then cleared -- except any you starred, noted or passed, which are never
cleared.

The neighbourhood filter no longer lists "San Francisco" as though it were a
neighbourhood beside Castro and Nob Hill.

The installer names each of its four steps instead of sitting on one unchanging
line while it fetches a private Python and the libraries it needs.


SF Home Finder 0.5.6 for Windows x64

Checks finish about forty seconds sooner. Sites that answer only for themselves -- the city's own
portal, Zumper, Uloop, AvalonBay, SpareRoom, Movoto and the building managers
-- are now read on a second line beside the two companies that count their
requests together (Zillow, Trulia and HotPads; Rent.com, ApartmentGuide and
Apartments.com), which stay strictly in turn. No site is asked for two things
at once or read any faster than before. SF_HOUSING_SEQUENTIAL_SCAN=1 puts
everything back in one line.

The rest of this release is a fix to the Mac's terminal command, which
Windows does not have: here the shortcuts in the folder you unpacked are the
way in.

Near matches means near. It used to hold everything that was not on the
shortlist; it now holds homes that missed by one thing -- a few points short
of your cut-off, or a little over your budget -- and each row says which, in
money, with the nearest miss at the top. Everything else collected is still in the full archive.

The Say thanks panel is tidier: the right size for what it shows, readable in
either theme, it says when it is loading, and it always offers a plain link to
Ko-fi in a browser tab in case their page will not draw inside the panel.

SF Home Finder 0.5.5 for Windows x64

Near matches means near. It used to hold every home that was not on the
shortlist, which on a real board meant 7,785 homes -- 7,625 of which the deal
had ruled out outright, for the wrong number of bedrooms or twice the budget.
It now holds only the homes that came within ten points of your cut-off.
Nothing is hidden; everything collected is still in the full archive.

Installing downloads about six megabytes less and installs ten fewer packages:
the app was asking for a bundle of extras it never used.

SF Home Finder 0.5.4 for Windows x64

Checking for new homes takes less time, the very first check included.
Craigslist runs on its own servers, so it is now read alongside the other
sources instead of ahead of them, and a check is shorter by about as long as
Craigslist takes. No site is asked for anything more than before: each is
still read one page at a time, and every other source is read in the same
order and at the same pace. SF_HOUSING_SEQUENTIAL_SCAN=1 puts every source back
in one line exactly as it was.

SF Home Finder 0.5.3 for Windows x64

Upgrading could fail part way through. Installing over a copy that is already
running means replacing files that copy has open, and Windows will not delete
an open file. The installer asked the app to stop, waited two seconds and
started anyway, which is usually enough and therefore fails exactly on the
slow and busy machines where an upgrade takes longest. Repair went through the
same path and could fail the same way. It now waits for the old copy to let
go, insists if it will not, and retries.

Everything else is as it was in 0.5.2, which was the first Windows build since
0.3.9. If you are coming from that one, read on.

SF Home Finder 0.5.2 for Windows x64

The first Windows build since 0.3.9. Everything below happened in between,
and all of it is new if you are coming from that one.

Start here: the 0.3.9 installer had been looking for a file that stopped
being built shortly afterwards, so a Windows install has not actually
worked for a long time. It does now, and it is checked on a real Windows
machine every time anything changes rather than read over on a Mac. Doing
that turned up three more faults, all fixed here: the installer gave up
waiting after 45 seconds when a first start can honestly take longer,
uninstalling failed part way through with a permission error because it
began deleting the app before the app had finished stopping, and opening
your browser at the end could fail an install that had otherwise worked.

It is called SF Home Finder now. The app always called itself that while
the download said SF Housing Monitor. Your data folder and the sign-in task
keep their old names on purpose, so nothing is orphaned.

It can tell you when a newer version exists. A line appears in small print
at the bottom of the app, and getting it is the same as installing it:
download the new release and run Install again, which keeps your deal, your
homes and your notes. The app asks GitHub once a day whether a newer
version exists and sends nothing about you, not even which version you are
running. SF_HOUSING_NO_UPDATE_CHECK=1 stops it entirely.

The number beside the match slider is right in three places it was wrong.
Raising your budget could not raise it. People splitting a home were shown
a fraction of what is there, because the rent each person pays was read as
the rent for the whole place. Naming neighborhoods made no difference at
all. Before you have searched anything it now estimates what a search like
yours usually finds instead of sitting at nothing.

Homes that have been taken down leave the shortlist promptly, and checking
by hand is limited to once a day so that no source stops answering you.

One thing macOS has that this does not: a homefinder command for the
terminal. Here the Install, Open, Repair, Verify and Uninstall shortcuts in
this folder are the way in, and Open is the one you will use.

SF Home Finder 0.3.9 for Windows x64

This Windows release uses the same local-only SF Home Finder application as
the macOS release. A blank install contains no housing profile, listings,
credentials, tokens, or personal paths.

The installer runs only for the signed-in Windows user. It downloads a private
Python 3.12 runtime, creates an app-owned environment under LocalAppData, and
starts the local dashboard at http://127.0.0.1:8000. It creates a normal user
sign-in task so scheduled checks can continue after the browser closes.

No optional connector blocks the account-free public-source workflow. Gmail is
read-only and needs an owner Google OAuth client plus real saved-search alerts
before individual providers are called working. Furnished Finder needs Chrome.
Facebook automation needs an optional Apify token and supports only the public
coverage described in Sources.

The ZIP is unsigned. Keep it private and only run a copy supplied by the owner.
