.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
# The developer's own database, profile and logs. Anchored to the repo root:
# sf_housing/data holds the read-only street table, which has to ship.
/data/
*.egg-info/
build/
dist/
*.zip.sha256

# Scaffolding for the open-source push; not part of the project.
OPEN_SOURCE_SPEC.md
.DS_Store
IMAP_SPEC.md
SPECS_TO_95.md
PRELAUNCH_WORK_ORDERS.md
TAR_XZ_SPEC.md

# Written by uv when the test and build commands run. The project has never
# tracked it, and nothing here reads it.
uv.lock

# Local state from testing the install counter worker.
deploy/install-counter/node_modules/
deploy/install-counter/.wrangler/
deploy/install-counter/package-lock.json

# Generated dashboard; regenerated by scripts/installs.py --page.
/reach.html

# Settings for the design tool used while working on the interface. They mean
# nothing to someone installing the app, so they stay on the machine that uses
# them rather than in the public repository.
/.impeccable/
